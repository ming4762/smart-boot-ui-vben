import type { SpaceProps } from 'antdv-next';

export interface SmartMultiInputProps {
  allowClear?: boolean;
  disabled?: boolean;
  placeholder?: string;
  size?: string;
  spaceProps?: SpaceProps;
  value?: string[];
}
