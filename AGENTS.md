# AGENTS.md

## 项目概况

- Vue 3、TypeScript、Vite、pnpm Monorepo。
- 业务应用位于 `apps`，共享包位于 `packages`，Smart Boot 业务模块位于 `smart-boot`。
- 优先复用项目已有组件、组合式函数、状态管理和国际化能力。

## 开发约定

- 修改前阅读目标文件及相邻实现，遵循现有目录结构、命名和代码风格。
- 优先做范围最小的修改，不顺带重构无关代码。
- 不覆盖或删除用户已有的未提交改动。
- 用户未明确要求时，不新增测试、不提交代码、不推送远程。
- 用户要求验证时，优先运行受影响应用或包的最小构建、类型检查或 lint。

## 代码注释要求

- 所有新增的 Vue 组件、TypeScript 类、接口、类型、组合式函数、Store、API 方法及非平凡函数必须添加 JSDoc 或用途注释。
- public、protected 方法以及对外导出的函数必须说明用途、参数、返回值和可能抛出的异常。
- 密码、认证、权限、令牌、加密、缓存、正则表达式等安全相关逻辑必须说明约束和设计原因。
- 复杂条件分支、兼容处理、边界条件和无法从代码直接看出的业务规则必须添加行内注释，重点解释“为什么这样处理”。
- 业务常量必须说明来源或用途；多个同类常量可以使用一段分组注释。
- 注释默认使用中文，术语、标识符和框架名称保留原文。
- 不为 import、简单赋值、显而易见的模板结构、普通 getter/setter 添加重复代码含义的无效注释。
- 修改既有函数时同步检查并更新其注释，禁止保留与实现不一致的注释。
- 交付前检查本次 diff，确认新增和修改的非平凡代码已包含必要注释。

## 注释示例

```ts
/**
 * 使用当前租户的密码规则校验新密码。
 *
 * @param password 待校验的明文密码
 * @returns 是否符合密码规则
 */
function validatePassword(password: string): boolean {
  // 未配置规则时按校验失败处理，避免绕过后台密码策略。
  return false;
}
```

## 交付说明

- 简要列出修改文件、行为变化和验证结果。
- 未执行的检查或已有阻塞需要明确说明。
