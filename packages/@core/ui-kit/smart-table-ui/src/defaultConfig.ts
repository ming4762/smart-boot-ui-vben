import type { SmartCheckboxConfig } from './types';

/**
 * 默认的checkbox配置
 */
const defaultCheckboxConfig: SmartCheckboxConfig = {
  rowCtrl: true,
  rowShift: true,
  rowTrigger: 'single',
};

export { defaultCheckboxConfig };
